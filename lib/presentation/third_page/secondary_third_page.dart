import 'package:flutter/material.dart';

class SecondaryThirdPage extends StatelessWidget {
  const SecondaryThirdPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Secondary page'));
  }
}
